import { LoginForm } from "../components/login-form";

const LoginPage = () => {
  return (
    <div className="w-full h-[calc(100vh-54px)] justify-center items-center flex">
    <LoginForm/>
    </div>
  );
};

export default LoginPage;
